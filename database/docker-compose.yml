

services:
  postgres:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Sai@3999
      POSTGRES_DB: fampay
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
